// Elements 61-70 have all been moved to elements58-71.ts (lanthanoids).
import { Element } from './elementTypes';

// Keep the file for compatibility, but export an empty array.
export const elements61to70: Element[] = [];
